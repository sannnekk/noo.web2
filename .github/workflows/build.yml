name: build

on:
  push:
    tags:
      - 'v*'

jobs:
  docker:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ vars.DOCKER_USER }}
          password: ${{ secrets.DOCKER_PAT }}
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
        with:
          driver: cloud
          endpoint: 'nooproject/noo-web-builder'
      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          platforms: linux/amd64
          # Plain single-arch manifest, so Docker Hub doesn't show an
          # extra unknown/unknown attestation entry next to the tag.
          provenance: false
          tags: |
            sannnekk/noo.front.new-alpha:latest
            sannnekk/noo.front.new-alpha:${{ github.ref_name }}
          outputs: type=registry
